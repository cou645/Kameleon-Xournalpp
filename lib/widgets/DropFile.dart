// Web-only drag-and-drop widget — stubbed out for Android build.
import 'package:flutter/material.dart';

class DropFile extends StatelessWidget {
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
